import React from 'react'
import { Subpage, Title } from '../../components'

interface Props {
    
}

const Home = (props: Props) => {
    return (
        <Subpage>
            <Title>Home</Title>
        </Subpage>
    )
}

export default Home
